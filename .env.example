# =============================================================================
# xg2g .env Example (Minimal)
# =============================================================================
# Copy to .env and adjust the few required values:
#   cp .env.example .env
#
# Configuration precedence:
#   ENV > config.yaml > defaults
# =============================================================================

# -----------------------------------------------------------------------------
# Required
# -----------------------------------------------------------------------------

# OpenWebIF base URL (REQUIRED)
XG2G_OWI_BASE=http://192.168.1.100

# API token (required; UI will prompt for this token)
# Generate: openssl rand -hex 32
XG2G_API_TOKEN=dev-token

# Scopes required for streaming
# Use "*" for dev or "v3:read,v3:write" for normal usage
XG2G_API_TOKEN_SCOPES=v3:read,v3:write

# Additional scoped tokens (JSON recommended; legacy token=scopes format still supported)
# XG2G_API_TOKENS=[{"token":"read-token","scopes":["v3:read"]},{"token":"ops-token","scopes":["v3:read","v3:write"]}]

# Secret for signing live playback decision tokens (recommended in multi-instance setups)
# Generate: openssl rand -hex 32
# XG2G_PLAYBACK_DECISION_SECRET=
# Optional key id (`kid`) for the active playback decision secret
# XG2G_PLAYBACK_DECISION_KID=kid-2026-02
# Optional comma-separated previous keys: "<kid>:<secret>"
# XG2G_PLAYBACK_DECISION_PREVIOUS_KEYS=kid-2026-01:oldsecret
# Rotation grace window for previous keys (duration)
# XG2G_PLAYBACK_DECISION_ROTATION_WINDOW=10m

# -----------------------------------------------------------------------------
# Common Optional
# -----------------------------------------------------------------------------

# Bouquet selection (comma-separated bouquet NAMES)
# XG2G_BOUQUET=

# Log level: debug, info, warn, error
# XG2G_LOG_LEVEL=info

# -----------------------------------------------------------------------------
# Streaming Engine (Optional)
# -----------------------------------------------------------------------------

# The streaming engine is enabled by default; set to false to disable.
# XG2G_ENGINE_ENABLED=true

# HLS output root (advanced). Default resolves to "$XG2G_DATA/hls" when unset.
# Prefer absolute paths in containers (e.g. /var/lib/xg2g/hls).
# XG2G_HLS_ROOT=./data/hls
