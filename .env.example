# shellcheck shell=sh
# shellcheck disable=SC2034
# xg2g Configuration Example
# Copy to .env and adjust values

# Required: Your Enigma2 receiver base URL
XG2G_OWI_BASE=http://192.168.1.100

# Required: Bouquet name(s) - single or comma-separated
XG2G_BOUQUET=Favourites

# Data directory for generated files
XG2G_DATA=/data

# XMLTV EPG file (relative to XG2G_DATA)
XG2G_XMLTV=xmltv.xml

# EPG settings
XG2G_EPG_ENABLED=true
XG2G_EPG_DAYS=7

# Stream port (default: 8001)
XG2G_STREAM_PORT=8001

# Fuzzy matching for EPG names
XG2G_FUZZY_MAX=2

# Picon base URL (optional, leave empty to disable)
XG2G_PICON_BASE=

# Metrics endpoint
XG2G_METRICS_LISTEN=:9090

# Optional: OpenWebIF authentication
# XG2G_OWI_USER=root
# XG2G_OWI_PASS=your-password
