# shellcheck shell=sh
# shellcheck disable=SC2034
# xg2g Configuration Example
# Copy to .env and adjust values
#
# ⚡ Standard 2025 setup - everything works out of the box!

# ============================================================================
# Required Configuration
# ============================================================================

# Your Enigma2 receiver
XG2G_OWI_BASE=http://192.168.1.100

# Enigma2 authentication (standard: root)
XG2G_OWI_USER=root
XG2G_OWI_PASS=your-password

# Bouquet name(s) - single or comma-separated
XG2G_BOUQUET=Favourites

# ============================================================================
# Feature Toggles (all enabled by default)
# ============================================================================

# EPG collection - 7 days by default
# XG2G_EPG_ENABLED=false          # Disable EPG if not needed
# XG2G_EPG_DAYS=7                 # Adjust days (1-14)

# Smart stream detection - automatically selects port 8001 or 17999 (OSCam)
# XG2G_SMART_STREAM_DETECTION=false    # Disable if you want manual port

# HDHomeRun emulation - Plex/Jellyfin auto-discovery
# XG2G_HDHR_ENABLED=false         # Disable if not using Plex/Jellyfin

# ============================================================================
# Advanced Configuration (rarely needed)
# ============================================================================

# Data directory for generated files
XG2G_DATA=/data

# XMLTV EPG file (relative to XG2G_DATA)
XG2G_XMLTV=xmltv.xml

# Fuzzy matching for EPG names
XG2G_FUZZY_MAX=2

# Picon base URL (optional, leave empty to disable)
XG2G_PICON_BASE=

# Metrics endpoint
XG2G_METRICS_LISTEN=:9090

# Stream Proxy Settings (Advanced)
# Enable if port 17999 doesn't support HEAD requests
# XG2G_ENABLE_STREAM_PROXY=false
# XG2G_PROXY_PORT=18000
# XG2G_PROXY_TARGET=http://192.168.1.100:17999
# XG2G_STREAM_BASE=http://your-host:18000

# Audio Transcoding Settings (Advanced)
# Fixes audio delay in browsers that don't support MP2/AC3
XG2G_ENABLE_AUDIO_TRANSCODING=false
XG2G_AUDIO_CODEC=aac
XG2G_AUDIO_BITRATE=192k
XG2G_AUDIO_CHANNELS=2
# XG2G_FFMPEG_PATH=ffmpeg

# HDHomeRun Customization (enabled by default)
# XG2G_HDHR_DEVICE_ID=XG2G1234
# XG2G_HDHR_FRIENDLY_NAME=xg2g
# XG2G_HDHR_MODEL=HDHR-xg2g
# XG2G_HDHR_FIRMWARE=xg2g-1.4.0
# XG2G_HDHR_BASE_URL=http://your-host:8080
# XG2G_HDHR_TUNER_COUNT=4

# API Token for Manual Refresh
# Generate with: openssl rand -hex 16
# XG2G_API_TOKEN=your-generated-token-here
