# shellcheck shell=sh
# shellcheck disable=SC2034
# xg2g Configuration
# Copy this file to .env and adjust values

# Required: Your Enigma2 receiver base URL
XG2G_OWI_BASE=http://192.168.1.100

# Required: Bouquet name(s) - single or comma-separated
# Examples: Favourites  or  Favourites,Movies,Sports
XG2G_BOUQUET=Favourites

# Prefer OpenWebIF /web/stream.m3u (receiver decides direct vs relay internally)
XG2G_USE_WEBIF_STREAMS=true

# Legacy: direct stream port (only used when XG2G_USE_WEBIF_STREAMS=false)
# XG2G_STREAM_PORT=8001

# Optional: OpenWebIF authentication (uncomment if needed)
# XG2G_OWI_USER=root
# XG2G_OWI_PASS=your-password

# XMLTV EPG settings
XG2G_XMLTV=xmltv.xml
XG2G_EPG_ENABLED=true
XG2G_EPG_DAYS=7

# Optional: Picon base URL (uncomment to enable channel logos)
# XG2G_PICON_BASE=${XG2G_OWI_BASE}/picon

# Advanced settings (defaults are fine for most users)
XG2G_DATA=/data
XG2G_LISTEN=:8080
XG2G_EPG_MAX_CONCURRENCY=5
XG2G_EPG_TIMEOUT_MS=15000
XG2G_EPG_RETRIES=2
XG2G_FUZZY_MAX=2
XG2G_METRICS_LISTEN=:9090
XG2G_OWI_TIMEOUT_MS=10000
XG2G_OWI_RETRIES=3
XG2G_OWI_BACKOFF_MS=500
