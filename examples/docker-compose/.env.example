# ============================================================================
# xg2g Configuration - Quick Setup
# Copy this file to .env and fill in the 4 required values below
# ============================================================================

# 1️⃣ What is your Enigma2 box IP address?
#    Example: http://10.10.55.57 or http://192.168.1.100
XG2G_OWI_BASE=http://YOUR_RECEIVER_IP_HERE

# 2️⃣ What is your bouquet name?
#    Find it in OpenWebif → EPG
#    Single: Premium
#    Multiple: Premium,Favourites,Sports
XG2G_BOUQUET=YOUR_BOUQUET_NAME_HERE

# 3️⃣ What stream port does your receiver use?
#    Most common: 8001
#    Alternative: 17999
XG2G_STREAM_PORT=8001

# 4️⃣ Does your OpenWebif require authentication?
#    If NO: Leave these lines commented (#)
#    If YES: Remove # and fill in username/password
# XG2G_OWI_USER=root
# XG2G_OWI_PASS=your-password-here

# ============================================================================
# AUTOMATIC FEATURES (enabled by default, change only if needed)
# ============================================================================

# ✅ XMLTV EPG (automatically fetches max available from receiver)
XG2G_XMLTV=xmltv.xml
XG2G_EPG_ENABLED=true
XG2G_EPG_DAYS=7

# ✅ Picons (channel logos from receiver)
# Auto-configured based on XG2G_OWI_BASE
# To disable: comment out the line below
XG2G_PICON_BASE=${XG2G_OWI_BASE}/picon

# ============================================================================
# ADVANCED SETTINGS (leave defaults)
# ============================================================================
XG2G_DATA=/data
XG2G_LISTEN=:8080
XG2G_EPG_MAX_CONCURRENCY=5
XG2G_EPG_TIMEOUT_MS=15000
XG2G_EPG_RETRIES=2
XG2G_FUZZY_MAX=2
XG2G_METRICS_LISTEN=:9090
XG2G_OWI_TIMEOUT_MS=10000
XG2G_OWI_RETRIES=3
XG2G_OWI_BACKOFF_MS=500
