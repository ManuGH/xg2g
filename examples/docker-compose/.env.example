# xg2g Environment Variables
# Copy this file and adjust the values

# === REQUIRED ===
# Base URL of your OpenWebIF receiver (VU+, Enigma2, etc.)
XG2G_OWI_BASE=http://192.168.1.100

# Bouquet name or ID (e.g. "Favourites", "1:7:1:0:0:0:0:0:0:0:FROM BOUQUET")
XG2G_BOUQUET=Favourites

# Data directory inside container
XG2G_DATA=/data

# Listen address
XG2G_LISTEN=:8080


# === XMLTV GENERATION (RECOMMENDED) ===
# If set, generates xmltv.xml file
XG2G_XMLTV=xmltv.xml


# === EPG (TV PROGRAM GUIDE) - PRIMARY FEATURE ===
# EPG loads full program information (what's playing when)
# IMPORTANT: Enable this for complete TV guide functionality!

# Enable EPG (true/false)
XG2G_EPG_ENABLED=true

# Number of days to load EPG data (1-14)
XG2G_EPG_DAYS=7

# Maximum parallel EPG requests (1-10, depends on receiver performance)
XG2G_EPG_MAX_CONCURRENCY=5

# Timeout per EPG request in milliseconds (5000-60000)
XG2G_EPG_TIMEOUT_MS=15000

# Retry attempts for failed EPG requests (0-5)
XG2G_EPG_RETRIES=2


# === OPTIONAL: PICON CONFIGURATION ===
# Base URL for picon images (channel logos)
# XG2G_PICON_BASE=http://192.168.1.100/picon


# === OPTIONAL: FUZZY MATCHING ===
# Maximum Levenshtein distance for EPG name matching (0-10)
XG2G_FUZZY_MAX=2


# === OPTIONAL: STREAM PORT ===
# Port for OpenWebIF streams (default: 8001)
# XG2G_STREAM_PORT=8001


# === OPTIONAL: PROMETHEUS METRICS ===
# Listen address for Prometheus metrics (empty = disabled)
XG2G_METRICS_LISTEN=:9090


# === OPTIONAL: RETRY CONFIGURATION ===
# Timeout per request in milliseconds (1000-60000)
XG2G_OWI_TIMEOUT_MS=10000

# Maximum number of retry attempts (0-10)
XG2G_OWI_RETRIES=3

# Base backoff delay in milliseconds (100-5000)
# Max backoff is automatically calculated: (2^retries) * backoff
XG2G_OWI_BACKOFF_MS=500
