# ============================================================================
# xg2g Live Test Konfiguration
# ============================================================================
#
# Kopiere diese Datei zu .env und passe die Werte an:
#   cp .env.livetest .env
#
# Dann starte den Stack mit:
#   docker-compose -f docker-compose.livetest.yml up -d
#
# ============================================================================

# ──────────────────────────────────────────────────────────────────────────
# Enigma2 Receiver Einstellungen (ERFORDERLICH)
# ──────────────────────────────────────────────────────────────────────────

# IP/Hostname deines Enigma2 Receivers (z.B. VU+, Dreambox, etc.)
XG2G_OWI_BASE=http://10.10.55.57

# Stream Port (Standard: 8001, Alternative bei Problemen: 17999)
XG2G_STREAM_PORT=8001

# OpenWebif Authentifizierung (wenn aktiviert)
XG2G_OWI_USER=root
XG2G_OWI_PASS=Kiddy99

# ──────────────────────────────────────────────────────────────────────────
# Bouquet Auswahl
# ──────────────────────────────────────────────────────────────────────────

# Einzelnes Bouquet
XG2G_BOUQUET=Premium

# Oder mehrere Bouquets kombiniert (komma-getrennt)
# XG2G_BOUQUET=Favourites,Movies,Sports

# Verfügbare Bouquets findest du hier:
# http://192.168.1.100/web/bouquets (im Browser)
# oder: curl http://192.168.1.100/api/bouquets

# ──────────────────────────────────────────────────────────────────────────
# EPG Einstellungen
# ──────────────────────────────────────────────────────────────────────────

# Anzahl Tage EPG (1-14)
XG2G_EPG_DAYS=7

# ──────────────────────────────────────────────────────────────────────────
# Zeitzone
# ──────────────────────────────────────────────────────────────────────────

TZ=Europe/Vienna

# ──────────────────────────────────────────────────────────────────────────
# Logging
# ──────────────────────────────────────────────────────────────────────────

# Log Level: debug, info, warn, error
XG2G_LOG_LEVEL=info

# Stream URL Format (Standard: false = Direkte TS Streams)
# XG2G_USE_WEBIF_STREAMS=false

# ──────────────────────────────────────────────────────────────────────────
# Tipps
# ──────────────────────────────────────────────────────────────────────────
#
# 1. Receiver muss im Standby (nicht Deep Standby) sein für Streaming
# 2. Firewall auf dem Receiver muss Ports 80 und 17999 erlauben
# 3. Nach dem Start:
#    - xg2g UI:      http://localhost:8080
#    - M3U Playlist: http://localhost:8080/files/playlist.m3u
#    - XMLTV EPG:    http://localhost:8080/files/xmltv.xml
#    - Threadfin:    http://localhost:34400
#    - Jellyfin:     http://localhost:8096
#
# ============================================================================
