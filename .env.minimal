# =============================================================================
# xg2g Minimal Configuration (Quick Start)
# =============================================================================
# This is the absolute minimum required to get xg2g running.
# For advanced options, see .env.example
# =============================================================================

# Your Enigma2 Receiver IP (REQUIRED)
XG2G_OWI_BASE=http://192.168.1.100

# Bouquet to stream (comma-separated names)
XG2G_BOUQUET=Favourites

# =============================================================================
# That's it! Run: docker compose up -d
# =============================================================================
# Optional: Secure your API with a token (recommended for production)
# XG2G_API_TOKEN=your-secret-token-here

# For more options (EPG, metrics, transcoding, etc.), see .env.example
