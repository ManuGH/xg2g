# xg2g Configuration Example
# Copy to .env and adjust values

# OpenWebIF Receiver Settings
XG2G_OWI_BASE=http://192.168.1.100          # Your OpenWebIF receiver URL
XG2G_BOUQUET=Premium                        # Bouquet name (e.g., "Premium", "Favorites")

# Application Settings
XG2G_DATA=/data                             # Data directory (persistent volume)
XG2G_LISTEN=:34400                          # HTTP listen address
XG2G_XMLTV=/data/guide.xml                  # XMLTV output path

# Optional: EPG Mapping
XG2G_FUZZY_MAX=2                            # Fuzzy matching distance (0=disabled)

# Optional: Picon Settings
XG2G_PICON_BASE=                            # Custom picon base URL (optional)

# Examples for different setups:
# XG2G_OWI_BASE=http://vuplus.local
# XG2G_BOUQUET="Favourites (TV)"
# XG2G_FUZZY_MAX=3
