# Canonical deploy bundle file for the optional VAAPI GPU overlay.
# Step-1 migration note: keep content aligned with repo-root docker-compose.gpu.yml
# until legacy verification and rendering paths are rewired.
services:
  xg2g:
    devices:
      - /dev/dri/renderD128:/dev/dri/renderD128
